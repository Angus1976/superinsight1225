{
  "name": "superdata",
  "private": true,
  "description": "Scripts so you can run npm from the repo root (frontend lives in ./frontend)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend"
  }
}
