# SuperInsight i18n 本地启动指南

## 系统要求

- Node.js 16+ (前端)
- Python 3.9+ (后端)
- Docker & Docker Compose (可选，用于完整部署)
- macOS/Linux/Windows WSL2

## 快速启动 (5分钟)

### 1. 启动后端服务

```bash
# 进入项目根目录
cd superinsight

# 安装 Python 依赖
pip install -r requirements.txt

# 启动 API 服务
python -m uvicorn src.app:app --host 0.0.0.0 --port 8000 --reload
```

**预期输出：**
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete
```

### 2. 启动前端应用

在新的终端窗口中：

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**预期输出：**
```
  VITE v4.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  press h to show help
```

### 3. 访问应用

- **前端**: http://localhost:5173
- **API**: http://localhost:8000
- **API 文档**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/health/i18n

---

## 测试账户

### 账户 1: 管理员 (Admin)
- **用户名**: admin@superinsight.com
- **密码**: Admin@123456
- **角色**: Administrator
- **权限**: 完全访问，包括系统设置、用户管理、i18n 配置
- **语言偏好**: 中文 (默认)

### 账户 2: 数据分析师 (Analyst)
- **用户名**: analyst@superinsight.com
- **密码**: Analyst@123456
- **角色**: Data Analyst
- **权限**: 数据查看、报表生成、分析工具
- **语言偏好**: 英文

### 账户 3: 内容编辑 (Editor)
- **用户名**: editor@superinsight.com
- **密码**: Editor@123456
- **角色**: Content Editor
- **权限**: 内容管理、翻译管理、发布权限
- **语言偏好**: 中文

### 账户 4: 普通用户 (User)
- **用户名**: user@superinsight.com
- **密码**: User@123456
- **角色**: Regular User
- **权限**: 基础功能访问、个人设置
- **语言偏好**: 英文

### 账户 5: 访客 (Guest)
- **用户名**: guest@superinsight.com
- **密码**: Guest@123456
- **角色**: Guest
- **权限**: 只读访问
- **语言偏好**: 中文

---

## 功能体验指南

### 1. 登录和语言切换

#### 步骤 1: 以管理员身份登录
1. 打开 http://localhost:5173
2. 输入用户名: `admin@superinsight.com`
3. 输入密码: `Admin@123456`
4. 点击登录

#### 步骤 2: 体验语言切换
1. 登录后，在右上角找到语言选择器
2. 点击"中文" → 观察界面切换为中文
3. 点击"English" → 观察界面切换为英文
4. 检查以下元素的翻译：
   - 导航菜单
   - 按钮标签
   - 表单标签
   - 错误消息

**预期结果**: 所有文本应该立即切换到相应的语言

### 2. 不同角色的功能体验

#### 体验 A: 管理员 (Admin)
**登录**: admin@superinsight.com / Admin@123456

**可用功能**:
- ✅ 系统设置 → 语言管理
- ✅ 用户管理 → 查看所有用户
- ✅ i18n 配置 → 管理翻译
- ✅ 监控面板 → 查看系统指标
- ✅ 审计日志 → 查看所有操作

**测试步骤**:
1. 导航到 "系统设置" → "语言管理"
2. 查看支持的语言列表
3. 尝试更改默认语言
4. 检查 API 响应中的 Content-Language 头

#### 体验 B: 数据分析师 (Analyst)
**登录**: analyst@superinsight.com / Analyst@123456

**可用功能**:
- ✅ 数据仪表板
- ✅ 报表生成
- ✅ 数据导出
- ✅ 分析工具
- ❌ 系统设置 (无权限)

**测试步骤**:
1. 登录后切换到英文
2. 导航到 "数据仪表板"
3. 生成一份报表
4. 验证所有文本都是英文
5. 尝试访问 "系统设置" (应该被拒绝)

#### 体验 C: 内容编辑 (Editor)
**登录**: editor@superinsight.com / Editor@123456

**可用功能**:
- ✅ 内容管理
- ✅ 翻译管理
- ✅ 发布管理
- ✅ 版本控制
- ❌ 用户管理 (无权限)

**测试步骤**:
1. 登录后保持中文
2. 导航到 "内容管理"
3. 创建新内容
4. 在 "翻译管理" 中添加英文翻译
5. 发布内容
6. 验证翻译是否正确应用

#### 体验 D: 普通用户 (User)
**登录**: user@superinsight.com / User@123456

**可用功能**:
- ✅ 个人仪表板
- ✅ 个人设置
- ✅ 语言偏好设置
- ✅ 查看公开内容
- ❌ 管理功能 (无权限)

**测试步骤**:
1. 登录后切换到英文
2. 导航到 "个人设置"
3. 更改语言偏好为英文
4. 刷新页面，验证语言保持为英文
5. 查看个人仪表板

#### 体验 E: 访客 (Guest)
**登录**: guest@superinsight.com / Guest@123456

**可用功能**:
- ✅ 查看公开内容
- ✅ 查看公开报表
- ✅ 语言切换
- ❌ 编辑功能 (无权限)
- ❌ 管理功能 (无权限)

**测试步骤**:
1. 登录后切换语言
2. 浏览公开内容
3. 尝试编辑内容 (应该被拒绝)
4. 验证只读访问

### 3. i18n 功能详细测试

#### 测试 3.1: 语言持久化
1. 以任何用户登录
2. 切换到英文
3. 刷新页面
4. **预期**: 语言应该保持为英文

#### 测试 3.2: API 语言检测
打开浏览器开发者工具 (F12)，查看网络请求：

```bash
# 在浏览器控制台中执行
fetch('http://localhost:8000/api/i18n/languages')
  .then(r => r.json())
  .then(d => console.log(d))
```

**预期响应**:
```json
{
  "supported_languages": ["zh", "en"],
  "default_language": "zh",
  "language_names": {
    "zh": "中文",
    "en": "English"
  }
}
```

#### 测试 3.3: 语言切换 API
```bash
# 切换到英文
fetch('http://localhost:8000/api/settings/language', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({language: 'en'})
})
  .then(r => r.json())
  .then(d => console.log(d))
```

**预期响应**:
```json
{
  "message": "Language updated successfully",
  "current_language": "en"
}
```

#### 测试 3.4: 翻译检索
```bash
# 获取中文翻译
fetch('http://localhost:8000/api/i18n/translations?language=zh')
  .then(r => r.json())
  .then(d => console.log(d.translations))
```

### 4. 性能和可靠性测试

#### 测试 4.1: 快速语言切换
1. 快速点击语言切换按钮 5-10 次
2. **预期**: 界面应该流畅响应，无延迟或错误

#### 测试 4.2: 并发请求
打开多个浏览器标签页，同时进行不同的操作：
- 标签页 1: 中文用户浏览数据
- 标签页 2: 英文用户浏览报表
- 标签页 3: 中文用户编辑内容

**预期**: 每个标签页应该独立维护自己的语言设置

#### 测试 4.3: 错误处理
1. 尝试访问不存在的翻译键
2. 尝试设置不支持的语言
3. **预期**: 应该显示友好的错误消息（中文或英文）

---

## API 测试命令

### 使用 curl 测试 i18n API

```bash
# 1. 获取支持的语言
curl http://localhost:8000/api/i18n/languages

# 2. 获取中文翻译
curl "http://localhost:8000/api/i18n/translations?language=zh"

# 3. 获取英文翻译
curl "http://localhost:8000/api/i18n/translations?language=en"

# 4. 获取当前语言设置
curl http://localhost:8000/api/settings/language

# 5. 切换到英文
curl -X POST http://localhost:8000/api/settings/language \
  -H "Content-Type: application/json" \
  -d '{"language": "en"}'

# 6. 健康检查
curl http://localhost:8000/health/i18n

# 7. 查看 API 文档
# 打开浏览器访问: http://localhost:8000/docs
```

---

## 故障排除

### 问题 1: 前端无法连接到后端
**症状**: 登录失败，显示连接错误

**解决方案**:
```bash
# 检查后端是否运行
curl http://localhost:8000/health/i18n

# 检查前端环境变量
cat frontend/.env.development

# 确保 VITE_API_URL 指向正确的后端地址
```

### 问题 2: 语言不切换
**症状**: 点击语言按钮后没有反应

**解决方案**:
```bash
# 检查浏览器控制台是否有错误
# 打开 F12 → Console 标签

# 检查 API 是否返回正确的翻译
curl "http://localhost:8000/api/i18n/translations?language=en"

# 清除浏览器缓存
# Ctrl+Shift+Delete (Windows) 或 Cmd+Shift+Delete (Mac)
```

### 问题 3: 登录失败
**症状**: 输入正确的用户名和密码后仍然登录失败

**解决方案**:
```bash
# 检查数据库是否有测试用户
# 查看后端日志中的错误信息

# 重新创建测试用户
python -c "
from src.models import User
from src.database import SessionLocal

db = SessionLocal()
user = User(
    email='admin@superinsight.com',
    username='admin',
    password_hash='hashed_password',
    role='admin'
)
db.add(user)
db.commit()
"
```

---

## 监控和调试

### 查看后端日志
```bash
# 实时查看日志
tail -f logs/app.log

# 查看 i18n 相关日志
grep "i18n" logs/app.log
```

### 查看前端日志
打开浏览器开发者工具 (F12) → Console 标签

### 性能监控
```bash
# 查看 API 响应时间
curl -w "\nTime: %{time_total}s\n" http://localhost:8000/api/i18n/languages

# 查看系统资源使用
top -p $(pgrep -f "uvicorn")
```

---

## 下一步

1. **部署到 Docker**: 使用 `deploy/i18n/docker-compose.yml`
2. **运行测试套件**: `pytest tests/test_i18n_*`
3. **性能测试**: `python performance_load_test.py`
4. **用户验收测试**: `python user_acceptance_test.py`

---

## 支持

如有问题，请查看：
- 📖 [用户指南](docs/i18n/user_guide.md)
- 🔧 [故障排除指南](docs/i18n/troubleshooting.md)
- 📚 [API 文档](docs/i18n/api_documentation.md)
- 🏗️ [架构文档](docs/i18n/architecture.md)